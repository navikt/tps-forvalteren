<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no http://maven.adeo.no/xsd/appconfig.xsd">

    <name>tps-forvalteren</name>

    <selftest>
        <path>/tps-forvalteren/internal/selftest</path>
        <humanReadablePath>/tps-forvalteren/internal/selftest</humanReadablePath>
    </selftest>

    <!--Consumed resources-->
    <resources>
        <webservice alias="virksomhet:Diskresjonskode_v1" mapToProperty="validering.virksomhet.diskresjonskodev1"/>
        <webservice alias="virksomhet:PipErEgenAnsatt_v1" mapToProperty="validering.virksomhet.egenansattv1"/>
        <ldap alias="ldap" mapToProperty="ldap"/>

        <queueManager alias="mqGateway" jndi="jms/mqConnectionFactory">
            <queue alias="TPS_FORESPORSEL_XML_O" jndi="jms/foresporsel_queue"/>
        </queueManager>

        <!-- DB trengs ikke i naavarende versjon. -->
        <!-- <datasource alias="tps-forvalterenDB" jndi="java:/jboss/datasources/tps-forvalterenDB"/> -->

        <applicationProperties alias="tps-forvalterenProperties"/>
    </resources>

    <security serviceUserResourceAlias="srvtps-forvalteren">
        <logins>
            <saml/>
        </logins>
    </security>

    <artifacts>
        <ear groupId="no.nav.tps.forvalteren" artifactId="tps-forvalteren-application-jee"/>
    </artifacts>
</application>