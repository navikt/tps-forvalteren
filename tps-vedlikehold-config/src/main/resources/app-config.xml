<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no http://maven.adeo.no/xsd/appconfig.xsd">

    <name>tps-vedlikehold</name>

    <selftest>
        <path>/tps-vedlikehold/internal/selftest</path>
        <humanReadablePath>/tps-vedlikehold/internal/selftest</humanReadablePath>
    </selftest>

    <!--Consumed resources-->
    <resources>
        <webservice alias="virksomhet:Diskresjonskode_v1" mapToProperty="validering.virksomhet.diskresjonskodev1"/>
        <webservice alias="virksomhet:PipErEgenAnsatt_v1" mapToProperty="validering.virksomhet.egenansattv1"/>
        <ldap alias="ldap" mapToProperty="ldap"/>

        <queueManager alias="mqGateway" jndi="jms/mqConnectionFactory">
            <queue alias="TPS_FORESPORSEL_XML_O" jndi="jms/foresporsel_queue"/>
        </queueManager>

        <datasource alias="tps-vedlikeholdDB" jndi="java:/jboss/datasources/tps-vedlikeholdDB"/>

        <applicationProperties alias="tps-vedlikeholdProperties"/>
    </resources>

    <security serviceUserResourceAlias="srvtps-vedlikehold">
        <logins>
            <saml/>
        </logins>
    </security>

    <artifacts>
        <ear groupId="no.nav.tps.vedlikehold" artifactId="tps-vedlikehold-application-jee"/>
    </artifacts>
</application>